services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: openrwa
      POSTGRES_PASSWORD: openrwa
      POSTGRES_DB: openrwa
    ports:
      - "5432:5432"
    volumes:
      - openrwa_pg:/var/lib/postgresql/data

volumes:
  openrwa_pg:
